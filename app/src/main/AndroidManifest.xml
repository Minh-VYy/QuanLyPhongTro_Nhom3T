<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuanLyPhongTro_ChuTro">

        <!--
             ===========================================================
             TẠM ẨN SPLASH SCREEN ĐỂ TEST CHỦ TRỌ
             ===========================================================
        -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <activity android:name=".ui.auth.LoginActivity" />

        <!-- Main Tenant Home (Guest Mode) -->
        <activity
            android:name=".ui.tenant.MainActivity"
            android:exported="false" />

        <!-- Auth Activities -->
        <activity android:name=".ui.auth.DangKyNguoiThueActivity" />
        <activity android:name=".ui.auth.DangKyChuTroActivity" />

        <!-- Tenant Activities -->
        <activity android:name=".ui.tenant.RoomDetailActivity" />
        <activity android:name=".ui.tenant.BookingCreateActivity" />
        <activity android:name=".ui.tenant.BookingListActivity" />
        <activity android:name=".ui.tenant.SavedRoomsActivity" />
        <activity android:name=".ui.tenant.NotificationsActivity" />
        <activity android:name=".ui.tenant.ProfileActivity" />
        <activity android:name=".ui.tenant.EditProfileActivity" />

        <!--
             ===========================================================
             LANDLORD ACTIVITIES (ĐANG BẬT CHẾ ĐỘ TEST)
             ===========================================================
        -->

        <!-- Thêm exported="true" và intent-filter vào đây để chạy trước -->
        <activity
            android:name=".ui.landlord.LandlordHomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.landlord.LandlordStatsActivity" />
        <activity android:name=".ui.landlord.LandlordProfileActivity" />
        <activity android:name=".ui.landlord.YeuCau" />
        <activity android:name=".ui.landlord.EditTin" />
        <activity android:name=".ui.landlord.TroGiup" />

        <!--
             ===========================================================
             LANDLORD ACTIVITIES (ĐANG BẬT CHẾ ĐỘ TEST)
             ===========================================================
        -->

        <!-- Thêm exported="true" và intent-filter vào đây để chạy trước -->
        <activity
            android:name=".ui.landlord.LandlordHomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.landlord.LandlordStatsActivity" />
        <activity android:name=".ui.landlord.LandlordProfileActivity" />
        <activity android:name=".ui.landlord.YeuCau" />
        <activity android:name=".ui.landlord.EditTin" />
        <activity android:name=".ui.landlord.TroGiup" />
        <activity android:name=".ui.landlord.AllListingsActivity" />

    </application>

</manifest>
